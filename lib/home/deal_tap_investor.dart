

import 'package:flutter/material.dart';

class DealInvestor extends StatelessWidget {
  const DealInvestor({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.pink,
    );
  }
}